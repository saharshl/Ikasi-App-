export const helper = {
  getAppVersion,
};

async function getAppVersion() {
  // const [{ appVersion }, update] = await Promise.all([codePush.getConfiguration(), codePush.getUpdateMetadata()]);
  // if (!update) {
  //   return `v${appVersion}`;
  // }
  // console.log(update);
  // const label = update.label.substring(1);
  // return `${appVersion} v.${label}`;
}
